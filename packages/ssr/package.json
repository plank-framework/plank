{
  "name": "@plank/ssr",
  "version": "0.1.0",
  "description": "Server-side rendering for Plank framework",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:coverage:ui": "vitest run --coverage --ui"
  },
  "dependencies": {
    "@plank/compiler": "workspace:*",
    "@plank/runtime-core": "workspace:*"
  },
  "devDependencies": {},
  "files": [
    "dist"
  ]
}
