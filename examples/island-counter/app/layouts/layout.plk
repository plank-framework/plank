<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><slot name="title" /> Island Counter - Plank Islands Demo</title>
  <meta name="description" content="Demonstration of Plank's islands architecture with interactive components.">

  <!-- Styles -->
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      line-height: 1.6;
      color: #1a1a1a;
      background: #ffffff;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    header {
      background: #ffffff;
      border-bottom: 1px solid #e5e5e5;
      padding: 1rem 0;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: #2563eb;
      text-decoration: none;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: #4b5563;
      text-decoration: none;
      font-weight: 500;
    }

    .nav-links a:hover {
      color: #2563eb;
    }

    main {
      min-height: calc(100vh - 140px);
      padding: 2rem 0;
    }

    footer {
      background: #f9fafb;
      padding: 2rem 0;
      text-align: center;
      color: #6b7280;
    }

    .hero {
      text-align: center;
      padding: 4rem 0;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 1rem;
    }

    .hero p {
      font-size: 1.25rem;
      margin-bottom: 2rem;
      opacity: 0.9;
    }

    .demo-section {
      padding: 4rem 0;
    }

    .demo-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .demo-card {
      background: #f9fafb;
      padding: 2rem;
      border-radius: 0.5rem;
      border: 1px solid #e5e5e5;
      text-align: center;
    }

    .demo-card h3 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: #1f2937;
    }

    .demo-card p {
      color: #6b7280;
      margin-bottom: 1.5rem;
    }

    .loading-placeholder {
      background: #e5e7eb;
      border-radius: 0.25rem;
      padding: 1rem;
      color: #6b7280;
      font-style: italic;
    }

    .island-info {
      background: #f0f9ff;
      border: 1px solid #0ea5e9;
      padding: 1.5rem;
      border-radius: 0.5rem;
      margin: 2rem 0;
    }

    .island-info h3 {
      color: #0c4a6e;
      margin-bottom: 0.5rem;
    }

    .island-info p {
      color: #075985;
      margin-bottom: 0.5rem;
    }

    .code-block {
      background: #1f2937;
      color: #f9fafb;
      padding: 1.5rem;
      border-radius: 0.5rem;
      overflow-x: auto;
      font-family: 'Monaco', 'Menlo', monospace;
      font-size: 0.875rem;
      margin: 1rem 0;
    }

    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2rem;
      }

      .nav-links {
        display: none;
      }

      .container {
        padding: 0 1rem;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="container">
      <nav>
        <a href="/" class="logo">Island Counter</a>
        <ul class="nav-links">
          <li><a href="/">Home</a></li>
          <li><a href="/about">About Islands</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <slot />
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2024 Plank Islands Demo. Interactive components load only when needed.</p>
    </div>
  </footer>
</body>
</html>
